<UserControl x:Class="MUI.Themes.Base64.Pages.Decoder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel MinWidth="200">
                <TextBox ToolTip="To paste, use Ctrl+V" TextWrapping="Wrap" Height="Auto" />
                <TextBlock/>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Save as" Width="Auto" HorizontalAlignment="Left" Click="Button_Click"/>
                    <TextBlock Text="" Foreground="{DynamicResource WindowTextReadOnly}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>


</UserControl>
